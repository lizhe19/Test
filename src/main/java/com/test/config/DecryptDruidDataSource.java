package com.test.config;

//import com.alibaba.druid.pool.DruidDataSource;
//import com.dahuatech.videoservices.common.utils.CommandUtil;
//import com.github.pagehelper.util.StringUtil;
import lombok.extern.slf4j.Slf4j;

/**
 * 数据库密码解密
 *
 * @author 119612
 * @version 1.0
 * @date 2020/08/20
 */
/*
@Slf4j
public class DecryptDruidDataSource extends DruidDataSource {

    @Override
    public void setPassword(String password) {
        password = CommandUtil.getCmdResult(password);
        if (StringUtil.isEmpty(password.trim())) {
            log.error("Password decrypt failed");
        }
        super.setPassword(password.trim());
    }
}*/
